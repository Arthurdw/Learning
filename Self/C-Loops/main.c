#include <stdio.h>

int main() {
    // int index = 0;

    // Normal While loop:
    // while (index <= 5) {
    //     printf("%d\n", index);
    //     index++;
    // }

    // do while in C:
    // do {
    //     printf("%d\n", index);
    //     index++;
    // } while (index <= 5)

    // Normal for loop:
    for (int i = 0; i <= 5; i++) {
        printf("%d\n", i);
    }

    return 0;
}
